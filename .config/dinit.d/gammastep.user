type            = process
load-options    = sub-vars
command         = /usr/bin/gammastep
smooth-recovery = true
logfile         = $HOME/.local/share/dinit/gammastep.log
depends-on      = geoclue.user
