# ~/.config/hypr/hyprpaper.conf
#
# monitor = (blank) → applies to ALL connected monitors automatically,
# including any external display hotplugged after startup.
# No IPC calls or monitor.lua helpers needed.

wallpaper {
    monitor  =
    path     = ~/pictures/zelda_arch.png
    fit_mode = cover
}
